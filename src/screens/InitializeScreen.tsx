import React from 'react';
import { View } from 'react-native';

/**
 * 概要：ログインしていないユーザの立ち上げ画面
 */
const InitializeScreen: React.FC = (): JSX.Element => {
  return <View />;
};

export default InitializeScreen;
